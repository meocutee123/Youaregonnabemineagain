@model Electronic_Store.Models.Brand

@{
    ViewBag.Title = "Details";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Details</h2>

<div class="container">
    <h4>Brand</h4>
    <hr />
    <div class="row">
        <div class="col-md-3">
            <img src="@Url.Content(Model.BrandImg)" />
        </div>
        <div class="col-md-6">
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.Name)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Name)
                </dd>

            </dl>
        </div>
    </div>

</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.BrandID }, new {@class="btn btn-md btn-success"}) |
    @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-md btn-danger" })
</p>
